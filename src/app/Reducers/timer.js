const timerState = (state=[], action) => {
    switch(action.type) {
        case('START_TIMER'):
            console.log(default, state);
        case('STOP_TIMER'):
            console.log(default, state);
        default:
            console.log(default, state)
    }
}